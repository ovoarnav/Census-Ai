# Toggle bundle-aware loading path.
USE_DOCUMENT_BUNDLES=true

# deterministic | heuristic_bundle | hf_with_fallback
EXTRACTION_MODE=deterministic

# If true, downstream consumers should treat unverified quotes as insufficient.
REQUIRE_VERIFIED_QUOTES=false
